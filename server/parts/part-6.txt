// Murdili Alpha Radar expansion: market regime, CEX listing intelligence,
// accumulation/breakout heuristics, deep flow and OHLCV charting.
sourceState.gecko=sourceState.gecko||{label:'GeckoTerminal',ok:false,lastOk:0,lastError:'',latency:null};
sourceState.cex=sourceState.cex||{label:'CEX Intel',ok:false,lastOk:0,lastError:'',latency:null};
sourceState.market=sourceState.market||{label:'Market Regime',ok:false,lastOk:0,lastError:'',latency:null};
const baseScoreTokenAlpha=scoreToken;scoreToken=function(token){const originalCex=token?.cex;if(token&&originalCex?.binanceMention)token.cex={...originalCex,binanceMention:false};try{return baseScoreTokenAlpha(token)}finally{if(token)token.cex=originalCex}};
const SOL_MINT='So11111111111111111111111111111111111111112',chartCache=new Map(),tradeCache=new Map();
const cexSymbolSets={Binance:new Set(),OKX:new Set(),Bybit:new Set(),Coinbase:new Set(),Kraken:new Set()};
const cexPages={OKX:{url:'https://www.okx.com/help/section/announcements-new-listings',kind:'official_listing_page',text:'',lastOk:0,lastError:''},Bybit:{url:'https://announcements.bybit.com/en/',kind:'official_listing_page',text:'',lastOk:0,lastError:''},Kraken:{url:'https://www.kraken.com/listings',kind:'official_roadmap',text:'',lastOk:0,lastError:''}};
let marketRegimeState={primary:'unknown',riskMode:'unknown',confidence:0,updatedAt:0,source:'',btc:null,sol:null,notes:[]};
async function fetchText(url,options={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),options.timeout||FETCH_TIMEOUT);try{const response=await fetch(url,{...options,headers:{accept:'text/html,application/xhtml+xml,text/plain,*/*','user-agent':'Mozilla/5.0 (compatible; MurdiliAlphaRadar/2.0; +https://murdilimax.com)',...(options.headers||{})},signal:controller.signal});if(!response.ok)throw new Error(`${response.status} ${response.statusText}`);return await response.text()}finally{clearTimeout(timer)}}
function normalizeSymbol(v){return str(v).toUpperCase().replace(/[^A-Z0-9]/g,'')}
function normalizePageText(html){return String(html||'').replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&nbsp;|&#160;/gi,' ').replace(/&amp;/gi,'&').replace(/&quot;/gi,'"').replace(/&#39;|&apos;/gi,"'").replace(/\s+/g,' ').trim()}
function pctChange(current,previous){current=num(current);previous=num(previous);return previous>0?((current-previous)/previous)*100:0}
function computeSetupIntel(token){const d=token.dex||{},timeline=Array.isArray(token.timeline)?token.timeline:[],currentMc=num(d.marketCap||token.pump?.marketCapUsd),currentLiq=num(d.liquidityUsd),currentVol=num(d.volumeH1),tx=num(d.txM5),buys=num(d.buysM5),sells=num(d.sellsM5),flowTotal=buys+sells,buyShare=flowTotal?buys/flowTotal:.5,change5m=num(d.changeM5),lookback=timeline.length>1?timeline[Math.max(0,timeline.length-8)]:null,mcChange=lookback?pctChange(currentMc,lookback.mc):change5m,liqChange=lookback?pctChange(currentLiq,lookback.liq):0,turnover=currentLiq>0?currentVol/currentLiq:0,compression=clamp(100-Math.abs(change5m)*5.5,0,100);let accumulation=0;const reasons=[];if(flowTotal>=8)accumulation+=clamp((buyShare-.45)*100,0,24);if(buyShare>=.56&&flowTotal>=10)reasons.push('buy_pressure');accumulation+=clamp(tx/1.4,0,18);if(tx>=20)reasons.push('active_tape');accumulation+=clamp(turnover*18,0,22);if(turnover>=.8)reasons.push('high_turnover');if(compression>=72&&currentVol>=Math.max(5000,currentLiq*.35)){accumulation+=18;reasons.push('price_absorption')}if(liqChange>3){accumulation+=clamp(liqChange*.8,0,12);reasons.push('liquidity_rising')}else if(liqChange<-12){accumulation-=18;reasons.push('liquidity_falling')}if(Math.abs(mcChange)<=8&&turnover>=.5&&tx>=12)reasons.push('compressed_with_flow');if(change5m>35)accumulation-=15;if(buyShare<.42&&flowTotal>=10)accumulation-=15;if(['high','critical'].includes(token.riskLevel))accumulation-=token.riskLevel==='critical'?30:15;accumulation=Math.round(clamp(accumulation,0,100));let breakout=accumulation*.55;breakout+=clamp(num(d.boosts)*1.2,0,10);breakout+=clamp(tx/2,0,12);if(change5m>=-3&&change5m<=12)breakout+=10;if(currentLiq>=10000)breakout+=8;if(buyShare>=.55&&buyShare<=.82)breakout+=8;if(change5m>45)breakout-=22;if(token.flags?.includes('vertical_price_move'))breakout-=18;if(['high','critical'].includes(token.riskLevel))breakout-=20;breakout=Math.round(clamp(breakout,0,100));let phase='neutral';if(accumulation>=72&&compression>=55)phase='accumulation';else if(breakout>=75&&change5m>=0&&change5m<35)phase='breakout_setup';else if(change5m>35)phase='expansion';else if(buyShare<.44&&flowTotal>=10)phase='distribution';return{accumulationPressure:accumulation,breakoutReadiness:breakout,priceCompression:Math.round(compression),phase,potentialPumpSetup:breakout>=75&&accumulation>=60&&!['high','critical'].includes(token.riskLevel),buySharePct:Math.round(buyShare*1000)/10,tx5m:tx,turnover1h:Math.round(turnover*100)/100,marketCapChangePct:Math.round(mcChange*100)/100,liquidityChangePct:Math.round(liqChange*100)/100,reasons:uniq(reasons),method:'Heuristic from order-flow proxy, turnover/liquidity, price compression, liquidity trend and risk. Not a price prediction.'}}
function tokenMatchesPage(token,pageText){const text=String(pageText||'');if(!text)return{matched:false};const symbol=normalizeSymbol(token.symbol),name=str(token.name).toLowerCase();let symbolMatch=false,nameMatch=false,index=-1;if(symbol.length>=2){const escaped=escapeRegExp(symbol),patterns=[new RegExp(`\\(${escaped}\\)`,'i'),new RegExp(`\\b${escaped}\\/USDT\\b`,'i'),new RegExp(`\\b${escaped}\\/USD\\b`,'i'),new RegExp(`\\b${escaped}USDT\\b`,'i')];if(symbol.length>=4)patterns.push(new RegExp(`\\b${escaped}\\b`,'i'));for(const p of patterns){const m=p.exec(text);if(m){symbolMatch=true;index=m.index;break}}}if(name.length>=5){const i=text.toLowerCase().indexOf(name);if(i>=0){nameMatch=true;if(index<0)index=i}}if(!symbolMatch&&!nameMatch)return{matched:false};return{matched:true,symbolMatch,nameMatch,excerpt:text.slice(Math.max(0,index-110),Math.min(text.length,index+220)).replace(/\s+/g,' ').trim()}}
function getCexIntel(token){const sym=normalizeSymbol(token.symbol),presence=[];if(sym)for(const[exchange,set]of Object.entries(cexSymbolSets))if(set.has(sym))presence.push({exchange,match:'symbol',contractVerified:false});const signals=[];for(const mention of token.cex?.mentions||[]){const title=str(mention.title),lower=title.toLowerCase(),name=str(token.name).toLowerCase(),exactName=name.length>=5&&lower.includes(name);signals.push({exchange:'Binance',kind:/will list|to list|listing|launch/i.test(title)?'official_announcement':'public_mention',evidenceScore:exactName?90:66,contractVerified:false,title,at:mention.releaseDate||0,url:'https://www.binance.com/en/support/announcement/list/48',note:exactName?'Official Binance announcement matches token name/ticker.':'Ticker/title match only; contract is not verified.'})}for(const[exchange,page]of Object.entries(cexPages)){const hit=tokenMatchesPage(token,page.text);if(!hit.matched)continue;const both=hit.symbolMatch&&hit.nameMatch,baseScore=page.kind==='official_roadmap'?82:74;signals.push({exchange,kind:page.kind,evidenceScore:clamp(baseScore+(both?12:0),0,95),contractVerified:false,title:page.kind==='official_roadmap'?`${exchange} official listings roadmap contains a matching asset reference`:`${exchange} official new-listings page contains a matching asset reference`,at:page.lastOk||0,url:page.url,excerpt:hit.excerpt,note:both?'Name and ticker matched on the official exchange page.':'Ticker/name match only; verify the exact contract.'})}signals.sort((a,b)=>b.evidenceScore-a.evidenceScore);const best=signals[0]||null;return{majorCexSymbolDetected:presence.length>0,noMajorCexSymbolDetected:presence.length===0,presence,signals:signals.slice(0,8),preCexCandidate:presence.length===0&&Boolean(best&&best.evidenceScore>=72),evidenceScore:best?.evidenceScore||0,evidenceLevel:best?(best.evidenceScore>=88?'strong_official_match':best.evidenceScore>=72?'official_page_match':'weak_match'):'none',caveat:'Major-CEX presence is checked primarily by ticker/symbol. Listing signals are official-page matches, but the exact Solana contract may remain unverified.'}}
function buyLinksFor(token){const mint=token.mint,links=[{id:'jupiter',label:'Jupiter',url:`https://jup.ag/swap?buy=${encodeURIComponent(mint)}&sell=${SOL_MINT}`,type:'aggregator'}];if(token.pump&&!token.pump.complete)links.push({id:'pump',label:'Pump.fun',url:`https://pump.fun/coin/${encodeURIComponent(mint)}`,type:'launchpad'});if(token.dex?.url)links.push({id:'dex',label:token.dex.dexId||'DEX',url:token.dex.url,type:'market'});return links}
function ema(values,period){if(!values?.length)return 0;const k=2/(period+1);let value=values[0];for(let i=1;i<values.length;i++)value=values[i]*k+value*(1-k);return value}
function stdev(values){if(!values?.length)return 0;const mean=values.reduce((a,b)=>a+b,0)/values.length;return Math.sqrt(values.reduce((s,x)=>s+Math.pow(x-mean,2),0)/values.length)}
async function fetchMarketCandles(symbol){const binance=symbol==='BTC'?'BTCUSDT':'SOLUSDT';try{const data=await fetchJson(`https://api.binance.com/api/v3/klines?symbol=${binance}&interval=1h&limit=168`,{timeout:8000});if(Array.isArray(data)&&data.length>=60)return{source:'Binance public market data',candles:data.map(x=>({at:num(x[0]),open:num(x[1]),high:num(x[2]),low:num(x[3]),close:num(x[4]),volume:num(x[5])}))}}catch{}const okx=symbol==='BTC'?'BTC-USDT':'SOL-USDT',data=await fetchJson(`https://www.okx.com/api/v5/market/candles?instId=${okx}&bar=1H&limit=168`,{timeout:8000}),rows=Array.isArray(data?.data)?data.data:[];if(!rows.length)throw new Error(`No ${symbol} market candles`);return{source:'OKX public market data',candles:rows.map(x=>({at:num(x[0]),open:num(x[1]),high:num(x[2]),low:num(x[3]),close:num(x[4]),volume:num(x[5])})).reverse()}}
function summarizeMarket(candles){const closes=candles.map(x=>x.close).filter(Boolean),price=closes.at(-1)||0,e20=ema(closes.slice(-80),20),e50=ema(closes.slice(-120),50),prev24=closes.at(-25)||closes[0]||price,prev7d=closes[0]||price,ret=[];for(let i=Math.max(1,closes.length-25);i<closes.length;i++)if(closes[i-1]>0)ret.push(Math.log(closes[i]/closes[i-1]));return{price,ema20:e20,ema50:e50,change24h:pctChange(price,prev24),change7d:pctChange(price,prev7d),volatility24h:stdev(ret)*Math.sqrt(24)*100}}
async function pollMarketRegime(){const started=now();try{const[btcRaw,solRaw]=await Promise.all([fetchMarketCandles('BTC'),fetchMarketCandles('SOL')]),btc=summarizeMarket(btcRaw.candles),sol=summarizeMarket(solRaw.candles),bullPoints=(btc.price>btc.ema20?1:0)+(btc.ema20>btc.ema50?1:0)+(btc.change24h>0?1:0)+(btc.change7d>0?1:0),bearPoints=(btc.price<btc.ema20?1:0)+(btc.ema20<btc.ema50?1:0)+(btc.change24h<0?1:0)+(btc.change7d<0?1:0),primary=bullPoints>=3?'bull':bearPoints>=3?'bear':'flat',rel=sol.change24h-btc.change24h,riskMode=rel>2&&sol.change24h>0?'risk_on':rel<-2&&sol.change24h<0?'risk_off':'neutral',confidence=Math.round(clamp(Math.max(bullPoints,bearPoints)/4*100,35,100)),notes=[];if(btc.price>btc.ema20&&btc.ema20>btc.ema50)notes.push('btc_above_ema_stack');if(btc.price<btc.ema20&&btc.ema20<btc.ema50)notes.push('btc_below_ema_stack');if(rel>2)notes.push('sol_outperforming_btc');if(rel<-2)notes.push('sol_underperforming_btc');if(btc.volatility24h>8)notes.push('high_btc_volatility');marketRegimeState={primary,riskMode,confidence,updatedAt:now(),source:`${btcRaw.source}; ${solRaw.source}`,btc,sol,notes,method:'Trend regime from BTC 1h EMA20/EMA50, 24h/7d direction and SOL relative strength. Descriptive, not predictive.'};touchSource('market',true,started)}catch(error){touchSource('market',false,started,error)}}
async function pollCexUniverse(){const started=now();let okCount=0;const tasks=[(async()=>{const data=await fetchJson('https://api.binance.com/api/v3/exchangeInfo',{timeout:10000});cexSymbolSets.Binance=new Set((data?.symbols||[]).filter(x=>['TRADING','PRE_TRADING'].includes(x.status)&&['USDT','USDC','FDUSD','USD'].includes(x.quoteAsset)).map(x=>normalizeSymbol(x.baseAsset)).filter(Boolean));okCount++})(),(async()=>{const data=await fetchJson('https://www.okx.com/api/v5/public/instruments?instType=SPOT',{timeout:10000});cexSymbolSets.OKX=new Set((data?.data||[]).map(x=>normalizeSymbol(x.baseCcy)).filter(Boolean));okCount++})(),(async()=>{const data=await fetchJson('https://api.bybit.com/v5/market/instruments-info?category=spot&limit=1000',{timeout:10000});cexSymbolSets.Bybit=new Set((data?.result?.list||[]).map(x=>normalizeSymbol(x.baseCoin)).filter(Boolean));okCount++})(),(async()=>{const data=await fetchJson('https://api.exchange.coinbase.com/products',{timeout:10000});cexSymbolSets.Coinbase=new Set((Array.isArray(data)?data:[]).map(x=>normalizeSymbol(x.base_currency)).filter(Boolean));okCount++})(),(async()=>{const data=await fetchJson('https://api.kraken.com/0/public/AssetPairs',{timeout:10000}),values=Object.values(data?.result||{});cexSymbolSets.Kraken=new Set(values.map(x=>normalizeSymbol(str(x?.wsname).split('/')[0]||x?.base)).filter(Boolean));okCount++})()];await Promise.allSettled(tasks);if(okCount)touchSource('cex',true,started);else touchSource('cex',false,started,'Major CEX symbol feeds unavailable');for(const page of Object.values(cexPages))try{const html=await fetchText(page.url,{timeout:12000}),text=normalizePageText(html);if(text.length<150)throw new Error('Official page returned too little text');page.text=text.slice(0,900000);page.lastOk=now();page.lastError=''}catch(error){page.lastError=compactError(error)}}
async function getChartData(token,tf='5m'){const allowed={'1m':['minute',1,120],'5m':['minute',5,144],'15m':['minute',15,144],'1h':['hour',1,168],'4h':['hour',4,180],'1d':['day',1,180]},keyTf=allowed[tf]?tf:'5m',[timeframe,aggregate,limit]=allowed[keyTf],pair=str(token.dex?.pairAddress);if(!pair)return{type:'market_cap',timeframe:keyTf,source:'Murdili observed snapshots',points:(token.timeline||[]).map(p=>[Math.floor(num(p.at)/1000),num(p.mc)])};const cacheKey=`${pair}:${keyTf}`,cached=chartCache.get(cacheKey);if(cached&&now()-cached.at<90000)return cached.value;const started=now();try{const url=`https://api.geckoterminal.com/api/v2/networks/solana/pools/${encodeURIComponent(pair)}/ohlcv/${timeframe}?aggregate=${aggregate}&limit=${limit}&currency=usd&token=${encodeURIComponent(token.mint)}`,data=await fetchJson(url,{timeout:10000}),rows=data?.data?.attributes?.ohlcv_list||[];if(!Array.isArray(rows)||!rows.length)throw new Error('No OHLCV');const candles=rows.map(x=>({at:num(x[0])*1000,open:num(x[1]),high:num(x[2]),low:num(x[3]),close:num(x[4]),volume:num(x[5])})).filter(x=>x.at&&x.close).reverse(),value={type:'ohlcv',timeframe:keyTf,source:'GeckoTerminal',pairAddress:pair,candles};chartCache.set(cacheKey,{at:now(),value});touchSource('gecko',true,started);return value}catch(error){touchSource('gecko',false,started,error);return{type:'market_cap',timeframe:keyTf,source:'Murdili observed snapshots (OHLCV unavailable)',error:compactError(error),points:(token.timeline||[]).map(p=>[Math.floor(num(p.at)/1000),num(p.mc)])}}}
async function getRecentPoolFlow(token){const pair=str(token.dex?.pairAddress);if(!pair)return null;const cached=tradeCache.get(pair);if(cached&&now()-cached.at<75000)return cached.value;const started=now();try{const data=await fetchJson(`https://api.geckoterminal.com/api/v2/networks/solana/pools/${encodeURIComponent(pair)}/trades?trade_volume_in_usd_greater_than=25`,{timeout:10000}),rows=Array.isArray(data?.data)?data.data:[],cutoff=now()-15*60000;let buyUsd=0,sellUsd=0,buys=0,sells=0;const wallets=new Set();for(const row of rows){const a=row?.attributes||{},ts=Date.parse(a.block_timestamp||'')||0;if(ts&&ts<cutoff)continue;const usd=num(a.volume_in_usd),kind=str(a.kind).toLowerCase();if(kind==='buy'){buyUsd+=usd;buys++}else if(kind==='sell'){sellUsd+=usd;sells++}if(a.tx_from_address)wallets.add(a.tx_from_address)}const total=buyUsd+sellUsd,value={windowMinutes:15,buyUsd:Math.round(buyUsd),sellUsd:Math.round(sellUsd),netBuyUsd:Math.round(buyUsd-sellUsd),buySharePct:total?Math.round(buyUsd/total*1000)/10:0,buys,sells,wallets:wallets.size,source:'GeckoTerminal recent trades',note:'Recent-pool trade flow; direction is relative to the selected token and can be noisy in thin markets.'};tradeCache.set(pair,{at:now(),value});touchSource('gecko',true,started);return value}catch(error){touchSource('gecko',false,started,error);return null}}
app.get('/api/market-regime',(_req,res)=>res.json({generatedAt:now(),regime:marketRegimeState,source:sourceState.market}));
app.get('/api/intel',(req,res)=>{const requested=str(req.query.mints).split(',').map(x=>x.trim()).filter(Boolean).slice(0,140),list=requested.length?requested.map(m=>tokens.get(m)).filter(Boolean):[...tokens.values()].sort((a,b)=>b.score-a.score).slice(0,120),intel={};for(const token of list)intel[token.mint]={setup:computeSetupIntel(token),cex:getCexIntel(token),buyLinks:buyLinksFor(token)};res.json({generatedAt:now(),marketRegime:marketRegimeState,intel})});
app.get('/api/intel/:mint',async(req,res)=>{const mint=str(req.params.mint),token=tokens.get(mint);if(!token)return res.status(404).json({error:'Token not found yet'});const deepFlow=await getRecentPoolFlow(token);res.json({generatedAt:now(),mint,setup:computeSetupIntel(token),deepFlow,cex:getCexIntel(token),buyLinks:buyLinksFor(token),marketRegime:marketRegimeState})});
app.get('/api/chart/:mint',async(req,res)=>{const mint=str(req.params.mint),token=tokens.get(mint);if(!token)return res.status(404).json({error:'Token not found yet'});const chart=await getChartData(token,str(req.query.tf)||'5m');res.json({generatedAt:now(),mint,chart})});
setTimeout(()=>{pollMarketRegime().catch(()=>{});pollCexUniverse().catch(()=>{})},2500);setInterval(()=>pollMarketRegime().catch(()=>{}),120000);setInterval(()=>pollCexUniverse().catch(()=>{}),15*60000);
