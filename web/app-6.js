// NEXUS ALPHA Extreme Asymmetry UI.
state.extremeData=state.extremeData||{dex:[],mexc:[],generatedAt:0};
Object.assign(I18N.ru,{extreme:'EXTREME',noExtreme:'Сейчас нет сильных Extreme Setup. Фильтр намеренно строгий.',extremeTitle:'EXTREME ASYMMETRY',extremeSub:'Сжатие + ускорение потока + глубина + risk gate',mexcLab:'MEXC ANOMALY LAB',mexcSub:'Структура до движения, а не уже случившийся памп',riskGate:'RISK GATE'});
Object.assign(I18N.en,{extreme:'EXTREME',noExtreme:'No strong Extreme Setup right now. The filter is intentionally strict.',extremeTitle:'EXTREME ASYMMETRY',extremeSub:'Compression + accelerating flow + depth + risk gate',mexcLab:'MEXC ANOMALY LAB',mexcSub:'Structure before expansion, not an already completed pump',riskGate:'RISK GATE'});
function extremeForMint(mint){return(state.extremeData?.dex||[]).find(x=>x.token?.mint===mint)?.extreme||null}
function gateClass(g){return g==='pass'?'gate-pass':g==='caution'?'gate-caution':'gate-fail'}
function fmtExtremePrice(v){v=Number(v)||0;if(!v)return'—';if(v>=100)return'$'+v.toLocaleString(undefined,{maximumFractionDigits:2});if(v>=1)return'$'+v.toLocaleString(undefined,{maximumFractionDigits:5});if(v>=.001)return'$'+v.toLocaleString(undefined,{maximumFractionDigits:7});return'$'+v.toExponential(3)}
function fmtExtremeMoney(v){v=Number(v)||0;if(v>=1e9)return'$'+(v/1e9).toFixed(1)+'B';if(v>=1e6)return'$'+(v/1e6).toFixed(1)+'M';if(v>=1e3)return'$'+(v/1e3).toFixed(0)+'K';return'$'+Math.round(v)}
function ensureExtremePanel(){
 const filters=$('tierFilters');if(filters&&!filters.querySelector('[data-tier="extreme"]'))filters.insertAdjacentHTML('beforeend',`<button class="filter extreme-filter" data-tier="extreme">✦ EXTREME</button>`);if(filters)filters.querySelectorAll('.filter').forEach(btn=>btn.onclick=()=>{filters.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));btn.classList.add('active');state.tier=btn.dataset.tier;renderTokens()});
 if($('extremeWorkspace'))return;const workspace=document.querySelector('.workspace');if(!workspace)return;workspace.insertAdjacentHTML('afterend',`<section id="extremeWorkspace" class="extreme-workspace"><article class="panel extreme-panel"><div class="panel-head"><div><span class="panel-kicker">ASYMMETRY ENGINE</span><h2>${esc(t('extremeTitle'))}</h2><p>${esc(t('extremeSub'))}</p></div><span class="blink-tag">LIVE</span></div><div id="extremeDexList" class="extreme-list"><div class="extreme-loading">Scanning…</div></div></article><article class="panel mexc-panel"><div class="panel-head"><div><span class="panel-kicker">CENTRALIZED MARKET MICROSTRUCTURE</span><h2>${esc(t('mexcLab'))}</h2><p>${esc(t('mexcSub'))}</p></div><span class="mexc-badge">MEXC</span></div><div id="mexcExtremeList" class="mexc-list"><div class="extreme-loading">Scanning MEXC…</div></div><p class="extreme-caveat">${state.lang==='ru'?'PASS означает только отсутствие критических флагов в доступных данных. Это не доказательство безопасности и не прогноз 100x/1000x.':'PASS only means no critical flag was found in available data. It is not proof of safety and not a 100x/1000x forecast.'}</p></article></section>`)
}
function renderExtremePanel(){ensureExtremePanel();const dexBox=$('extremeDexList'),mexcBox=$('mexcExtremeList');if(!dexBox||!mexcBox)return;const dex=(state.extremeData.dex||[]).filter(x=>x.extreme?.score>=55&&x.extreme?.riskGate!=='fail').slice(0,8);dexBox.innerHTML=dex.length?dex.map(x=>{const tk=x.token,e=x.extreme,s=e.setup||{};return`<button class="extreme-row" data-extreme-mint="${esc(tk.mint)}"><div class="extreme-token"><img src="${esc(tk.image||'./icon.svg')}" alt="" onerror="this.src='./icon.svg'"><div><strong>${esc(tk.symbol||tk.name||'TOKEN')}</strong><span>${esc(tk.name||shortAddr(tk.mint))}</span></div></div><div class="extreme-score"><b>${e.score}</b><span>ASYMMETRY</span></div><div><b>${s.accumulationPressure??'—'}</b><span>ACCUM.</span></div><div><b>${s.priceCompression??'—'}</b><span>COMPRESS.</span></div><div><b>${s.buySharePct!=null?s.buySharePct+'%':'—'}</b><span>BUY FLOW</span></div><span class="gate ${gateClass(e.riskGate)}">${esc(e.riskGate.toUpperCase())}</span></button>`}).join(''):`<div class="extreme-empty">${esc(t('noExtreme'))}</div>`;dexBox.querySelectorAll('[data-extreme-mint]').forEach(el=>el.onclick=()=>{const tk=(state.data?.tokens||[]).find(x=>x.mint===el.dataset.extremeMint)||(state.alphaExtendedTokens||[]).find(x=>x.mint===el.dataset.extremeMint);if(tk)renderDetail(tk)});
 const mexc=(state.extremeData.mexc||[]).filter(x=>x.extreme?.score>=40).slice(0,12);mexcBox.innerHTML=mexc.length?mexc.map(m=>{const e=m.extreme,k=m.deep?.kline||{};return`<a class="mexc-row" href="${esc(m.url||'#')}" target="_blank" rel="noopener noreferrer"><div><strong>${esc(m.base)}/${esc(m.quote)}</strong><span>${fmtExtremePrice(m.price)} · 24h ${Number(m.change24h)>=0?'+':''}${Number(m.change24h||0).toFixed(1)}%</span></div><div><b>${e.score||0}</b><span>SETUP</span></div><div><b>${e.authenticity||0}</b><span>AUTH</span></div><div><b>${k.volAccel?Number(k.volAccel).toFixed(1)+'×':'—'}</b><span>VOL ACCEL</span></div><div><b>${m.deep?.bidAskImbalance2?Number(m.deep.bidAskImbalance2).toFixed(2):'—'}</b><span>BID/ASK</span></div><div><b>${fmtExtremeMoney(m.deep?.depth2Usd||0)}</b><span>DEPTH ±2%</span></div><span class="gate ${gateClass(e.riskGate)}">${esc(e.riskGate.toUpperCase())}</span></a>`}).join(''):`<div class="extreme-empty">${state.lang==='ru'?'MEXC deep-scan набирает свечи и стаканы. Тонкие рынки намеренно не показываются.':'MEXC deep scan is collecting candles and books. Thin markets are intentionally hidden.'}</div>`}
const extremeFilteredBase=filteredTokens;filteredTokens=function(){let list=extremeFilteredBase();if(state.tier==='extreme')list=list.filter(x=>{const e=extremeForMint(x.mint);return e&&e.score>=60&&e.riskGate!=='fail'});return list};
const extremeEmptyBase=nexusEmptyMessage;nexusEmptyMessage=function(){if(state.tier==='extreme')return t('noExtreme');return extremeEmptyBase()};
const extremeRenderTokensBase=renderTokens;renderTokens=function(){extremeRenderTokensBase();document.querySelectorAll('#tokenBody tr[data-mint]').forEach(row=>{const e=extremeForMint(row.dataset.mint);if(!e||e.score<55)return;const cell=row.querySelector('td:last-child');if(cell&&!cell.querySelector('.edge-chip.extreme'))cell.insertAdjacentHTML('beforeend',`<div class="edge-chip-row"><span class="edge-chip extreme">✦ EXT ${e.score}</span><span class="edge-chip ${gateClass(e.riskGate)}">${esc(e.riskGate.toUpperCase())}</span></div>`)})};
const extremeDetailBase=renderDetail;renderDetail=function(tk){extremeDetailBase(tk);const e=extremeForMint(tk.mint);if(!e)return;setTimeout(()=>{if(state.selectedMint!==tk.mint)return;const root=$('drawerContent');if(!root||root.querySelector('.extreme-detail'))return;const setup=e.setup||{},reasons=(e.reasons||[]).map(x=>`<span>${esc(x.replaceAll('_',' '))}</span>`).join('');root.querySelector('.drawer-score-grid')?.insertAdjacentHTML('afterend',`<section class="drawer-section extreme-detail"><h3>✦ EXTREME ASYMMETRY</h3><div class="intel-grid"><div><span>SETUP</span><strong>${e.score}/100</strong><small>ranking</small></div><div><span>ACCUMULATION</span><strong>${setup.accumulationPressure??'—'}</strong><small>flow</small></div><div><span>COMPRESSION</span><strong>${setup.priceCompression??'—'}</strong><small>price</small></div><div><span>${esc(t('riskGate'))}</span><strong class="${gateClass(e.riskGate)}">${esc(e.riskGate.toUpperCase())}</strong><small>not a guarantee</small></div></div><div class="extreme-reasons">${reasons}</div><p class="drawer-note">${state.lang==='ru'?'Высокий балл означает редкую комбинацию признаков до движения. Он не означает, что токен обязательно вырастет, и PASS не доказывает отсутствие мошенничества.':'A high score means an unusual pre-move configuration. It does not mean the token will rise, and PASS does not prove absence of fraud.'}</p></section>`)},250)};
const extremeAlertTitleBase=alertTitle;alertTitle=function(a){if(a?.title==='MEXC_EXTREME_SETUP')return state.lang==='ru'?'MEXC: необычная структура до импульса':'MEXC: unusual pre-impulse structure';return extremeAlertTitleBase(a)};
async function refreshExtreme(){try{const r=await fetch(`${API_BASE}/api/extreme`,{cache:'no-store'});if(!r.ok)throw new Error('extreme');state.extremeData=await r.json();for(const item of state.extremeData.dex||[]){const mint=item.token?.mint;if(!mint)continue;[...(state.data?.tokens||[]),...(state.alphaExtendedTokens||[])].filter(x=>x.mint===mint).forEach(x=>x.extreme=item.extreme)}renderExtremePanel();renderTokens()}catch{renderExtremePanel()}}
const extremeStyle=document.createElement('style');extremeStyle.textContent=`.extreme-workspace{display:grid;grid-template-columns:1fr 1.15fr;gap:12px;margin:12px 0}.panel-head p{margin:4px 0 0;color:var(--muted);font-size:9px}.extreme-list,.mexc-list{padding:0 12px 12px}.extreme-row,.mexc-row{width:100%;display:grid;align-items:center;gap:9px;border:0;border-top:1px solid var(--line);background:transparent;color:var(--text);padding:10px 3px;text-align:left;text-decoration:none;cursor:pointer}.extreme-row{grid-template-columns:minmax(120px,1.8fr) repeat(4,minmax(54px,.7fr)) auto}.mexc-row{grid-template-columns:minmax(110px,1.5fr) repeat(5,minmax(55px,.7fr)) auto}.extreme-row:hover,.mexc-row:hover{background:rgba(93,228,255,.035)}.extreme-token{display:flex;align-items:center;gap:8px;min-width:0}.extreme-token img{width:30px;height:30px;border-radius:9px;object-fit:cover}.extreme-token strong,.mexc-row strong{display:block;font-size:10px}.extreme-token span,.extreme-row div>span,.mexc-row div>span{display:block;color:var(--muted);font-size:7px;margin-top:3px}.extreme-row div>b,.mexc-row div>b{font-size:11px}.extreme-score b{color:#d8ff75;font-size:17px!important}.gate{justify-self:end;border:1px solid var(--line);border-radius:999px;padding:4px 6px;font-size:7px;font-weight:800}.gate-pass,.edge-chip.gate-pass{color:#73f2b8!important;border-color:rgba(115,242,184,.3)!important}.gate-caution,.edge-chip.gate-caution{color:#ffd17b!important;border-color:rgba(255,209,123,.3)!important}.gate-fail{color:#ff8383!important;border-color:rgba(255,131,131,.3)!important}.edge-chip.extreme{color:#d8ff75;border-color:rgba(216,255,117,.3);background:rgba(216,255,117,.06)}.mexc-badge{font-size:8px;font-weight:900;border:1px solid rgba(124,99,255,.35);padding:5px 7px;border-radius:999px;color:#c7bbff}.extreme-empty,.extreme-loading{padding:18px 4px;color:var(--muted);font-size:9px}.extreme-caveat{font-size:8px;line-height:1.55;color:var(--muted);padding:0 14px 12px;margin:0}.extreme-reasons{display:flex;gap:5px;flex-wrap:wrap;margin:10px 0}.extreme-reasons span{font-size:7px;border:1px solid var(--line);border-radius:999px;padding:4px 6px;color:#bcd4df}@media(max-width:1180px){.extreme-workspace{grid-template-columns:1fr}.mexc-row{grid-template-columns:minmax(110px,1.5fr) repeat(3,minmax(55px,.7fr)) auto}.mexc-row>div:nth-of-type(5),.mexc-row>div:nth-of-type(6){display:none}}@media(max-width:760px){.extreme-row{grid-template-columns:minmax(110px,1.8fr) repeat(2,minmax(52px,.7fr)) auto}.extreme-row>div:nth-of-type(4),.extreme-row>div:nth-of-type(5){display:none}.mexc-row{grid-template-columns:minmax(105px,1.6fr) 55px 55px auto}.mexc-row>div:nth-of-type(4),.mexc-row>div:nth-of-type(5),.mexc-row>div:nth-of-type(6){display:none}}`;document.head.appendChild(extremeStyle);setTimeout(()=>{ensureExtremePanel();refreshExtreme()},1000);setInterval(refreshExtreme,45_000);
